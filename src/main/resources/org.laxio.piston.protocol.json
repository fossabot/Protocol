{
  "Implementation-Title": "${project.name}",
  "Implementation-Version": "${describe}",
  "Implementation-Vendor": "${project.organisation.name}",
  "Specification-Title": "${project.name}",
  "Specification-Version": "${describe}",
  "Specification-Vendor": "${project.organisation.name}"
}